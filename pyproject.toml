[project]
name = "daily-reflection-system"
version = "0.1.0"
description = "Reconstruct your day from lifelog screenshots via VLM classification"
requires-python = ">=3.10"
dependencies = [
    "imagehash>=4.3",
    "Pillow>=10.0",
]

[project.scripts]
daily-reflect = "daily_reflect.main:main"
